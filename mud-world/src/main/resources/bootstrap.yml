spring:
  application:
    name: mud-world

management:
  info:
    build:
      enabled: true
      
  endpoint:
    info:
      enabled: true

---

spring:
  profiles: itg, itgtest

  cloud:
    config:
      uri: http://jeremias:8888
      
---

spring:
  profiles: prod 

  cloud:
    config:
      uri: ${CONFIG_SERVER_URL} 
      