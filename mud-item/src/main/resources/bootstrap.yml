spring:
  cloud:
    config:
      uri: ${CONFIG_SERVER_URL:http://jeremias:8888} 
      
  application:
    name: mud-item

management:
  info:
    build:
      enabled: true
      
endpoints:
  info:
    enabled: true
    sensitive: false